# Circle Frame Studio v0.4 업데이트 계획서

## 1. 업데이트 목표

v0.4의 최우선 목표는 **편집 화면에서 보이는 프레임과 2000×2000 PNG로 저장되는 프레임을 동일하게 만드는 것**입니다.

v0.3에서는 미리보기 캔버스가 1200×1200으로 렌더링되고, 최종 PNG는 2000×2000으로 다시 렌더링되었습니다. 기본 원형 선은 비율이 비슷하게 보이지만 Glow, Blur, Shadow, 사용자 PNG 장식처럼 픽셀 단위 효과가 포함된 디자인에서는 두 렌더 환경의 차이가 생길 수 있습니다. 또한 Auto Fit의 외곽 크기 계산이 실제 블러 확산 범위보다 작아 일부 디자인이 저장 결과에서 잘릴 가능성이 있었습니다.

v0.4에서는 이 두 문제를 함께 해결합니다.

---

## 2. 미리보기와 PNG 출력 렌더 기준 통일

### v0.3
- 편집 미리보기 내부 캔버스: 1200×1200
- PNG 출력 캔버스: 2000×2000
- 같은 수식을 사용하지만 실제 Canvas 픽셀 수가 달랐음

### v0.4
- 편집 미리보기 내부 캔버스도 **실제 2000×2000**으로 변경
- 화면에서는 CSS로 축소 표시
- PNG 저장도 동일한 2000×2000 렌더러 사용

즉 편집 화면은 단순한 저해상도 프록시가 아니라 **최종 PNG와 같은 해상도의 결과물을 축소해서 보여주는 미리보기**가 됩니다.

이 변경으로 다음 효과의 화면/출력 차이를 최소화합니다.

- 원형 선 두께
- 낙서/브러시 선
- 하트/리본/꽃 반복 장식
- 사용자 PNG 반복 장식
- Glow
- Bloom
- Soft Blur
- Shadow
- Gradient

---

## 3. Auto Fit 안전 영역 계산 개선

v0.3의 `estimateDesignExtent()`는 여러 효과를 단순 합산하는 방식이었지만 일부 항목의 실제 확산 영역을 충분히 크게 잡지 못했습니다.

v0.4에서는 프레임 종류별 외곽 범위를 따로 계산합니다.

### 기본/이중/삼중 원
- 반지름
- 실제 선 두께의 절반
- 이중/삼중 링 사이 간격까지 반영

### 낙서/러프/브러시
- 거칠기
- 여러 번 겹쳐 그리는 Stroke 간의 외곽 이동
- 브러시 두께 변화량

### Wavy / Scallop
- 물결 높이를 실제 외곽 반경에 포함

### 장식 프레임
- 장식이 배치되는 원의 반지름
- 장식 거리 Offset
- 장식 크기

### 효과
- Glow Blur의 실제 확산 여유를 더 크게 계산
- Shadow Blur + Shadow Offset 계산
- Soft Blur를 다중 blur-radius 기준으로 계산
- Bloom blur 확산 범위 계산

Auto Fit이 켜져 있을 때 최종 PNG에는 **최소 약 50px의 투명 안전 여백**을 남기도록 설계합니다.

---

## 4. 업로드 PNG 프레임 크기 계산 수정

v0.3에서는 사용자 PNG 장식의 높이를 기준으로 크기를 설정하고 가로는 원본 비율에 따라 확장했습니다.

따라서 지나치게 가로로 긴 PNG를 넣으면 사용자가 설정한 장식 크기보다 훨씬 넓게 렌더링될 수 있었습니다.

v0.4에서는:

- `장식 크기`를 업로드 이미지의 **가장 긴 변 기준**으로 사용
- 원본 종횡비는 그대로 보존
- 세로형/가로형 PNG 모두 지정 크기 안에 들어오도록 조정

이 방식은 사용자 PNG 반복 프레임의 외곽 범위를 예측 가능하게 만들고 저장 시 잘림도 줄입니다.

---

## 5. Preview = Export 표시

미리보기 상단에 다음 상태 표시를 추가합니다.

`미리보기 = PNG 출력 1:1 렌더 기준`

여기서 1:1은 화면 표시 크기가 2000px이라는 뜻이 아니라, **내부 렌더링 해상도와 계산 방식이 PNG 출력과 동일하다**는 뜻입니다.

---

## 6. v0.3 프로젝트/프리셋 호환

v0.4는 새 자동 저장 키를 사용하지만 브라우저에 v0.3 데이터만 남아 있다면 자동으로 v0.3 저장 데이터를 읽어옵니다.

- v0.4 프로젝트 자동 저장 우선
- 없으면 v0.3 자동 저장을 불러와 v0.4 구조로 정규화
- v0.3 사용자 프리셋도 동일 방식으로 이어받음
- v0.1/v0.2 JSON 호환 로직도 유지

---

## 7. 저장 규격

- 최종 PNG: **2000×2000px**
- 배경: 완전 투명 RGBA
- 파일명: `circle-frame-v04-YYYY-MM-DD.png`
- 프로젝트 JSON: `circle-frame-project-v04.json`

---

## 8. v0.4 완료 조건

1. 미리보기 내부 Canvas와 최종 PNG가 모두 2000×2000 렌더 기준을 사용한다.
2. 같은 디자인에서 화면과 저장 파일의 프레임 크기/위치/효과가 동일하게 보인다.
3. Auto Fit ON 상태에서 두꺼운 링, 장식, Glow, Shadow, Blur가 PNG 가장자리에 잘리지 않는다.
4. 하트/리본/꽃 반복 프레임도 안전하게 저장된다.
5. 사용자가 업로드한 투명 PNG 반복 프레임도 안전하게 저장된다.
6. 가로로 긴 사용자 PNG도 장식 크기 설정 범위 안에서 렌더링된다.
7. v0.3 자동 저장과 사용자 프리셋을 이어서 사용할 수 있다.
8. GitHub + Cloudflare Pages 구조를 그대로 유지한다.
