# Circle Frame Studio v0.3 업데이트 계획서

## 1. 업데이트 목적

v0.2에서 프리셋을 클릭할 때마다 새로운 레이어가 추가되는 방식은, **한 번에 원형 프레임 하나만 만들어 사용하는 실제 사용 흐름**에 비해 복잡했습니다. 또한 사용자 업로드 이미지 반복 기능이 세부 설정 안쪽에 있어 발견하기 어려웠고, 하트·리본·꽃처럼 반복되는 장식 프레임은 단색 또는 2색 수준에서 끝나 색 조합 자유도가 부족했습니다.

v0.3의 목표는 편집 구조를 단순하게 만들면서도 반복 패턴 프레임 제작 기능은 더 강하게 만드는 것입니다.

핵심 방향은 다음 세 가지입니다.

1. **레이어 시스템 제거 → 항상 프레임 하나만 편집**
2. **투명 PNG 업로드 기능을 화면 전면에 노출**
3. **반복 장식에 1색 / 2색 / 3색 순환 팔레트 적용**

---

## 2. 단일 프레임 편집 구조로 전환

### 기존 문제
- 프리셋을 누를 때마다 프레임이 추가됨
- 이전 프레임이 계속 남아 사용자가 직접 삭제해야 함
- 한 프레임만 필요한 사용자에게 레이어 패널이 불필요함

### v0.3 변경
- 레이어 패널 완전 제거
- 프로젝트에는 `design` 하나만 존재
- 프리셋 클릭 시 현재 디자인을 새 프리셋으로 **즉시 교체**
- 프리셋을 여러 번 클릭해도 화면에는 항상 하나의 프레임만 존재
- 실행취소/다시실행은 그대로 유지하여 실수로 프리셋을 바꿔도 되돌릴 수 있음

### 결과
사용 흐름이 다음처럼 단순해집니다.

`프리셋 선택 → 색상/크기/효과 수정 → PNG 저장`

---

## 3. “내 PNG로 반복 프레임 만들기” 전용 업로드 영역

### 화면 위치
왼쪽 프리셋 패널 **최상단**에 별도의 큰 업로드 박스를 둡니다.

표시 문구:
- `내 PNG로 반복 프레임 만들기`
- `투명 하트 · 리본 · 꽃 · 스티커`
- `투명 PNG / WebP / SVG 선택`

### 지원 방식
- 클릭하여 파일 선택
- 파일을 업로드 박스로 드래그 앤 드롭
- 업로드 즉시 `내 PNG 반복 프레임`으로 현재 디자인 교체
- 업로드한 이미지가 원 둘레에 반복 배치됨

### 지원 파일
- PNG
- WebP
- SVG

### 자동 최적화
큰 이미지를 그대로 프로젝트 JSON에 넣으면 브라우저 저장 용량 문제가 생길 수 있으므로 업로드 시:
- 최대 512px 기준으로 자동 축소
- 투명 배경 유지
- 내부 저장용 PNG 데이터로 변환

---

## 4. 사용자 이미지 반복 프레임 편집 기능

업로드한 이미지에 다음 값을 실시간으로 조절할 수 있습니다.

- 원 크기
- 장식 크기
- 장식 간격
- 장식 개수
- 원에서 장식까지의 거리
- 배치 시작 각도
- 전체 회전
- 장식 정방향 유지 ON/OFF
- 불투명도
- Glow / Neon
- Bloom
- Soft Blur
- Shadow

### 간격 조절 방식 2종
사용자가 원하는 방식에 따라 선택합니다.

**A. 간격(px) 기준**
- 숫자가 작을수록 촘촘하게 배치
- 숫자가 클수록 넓게 배치
- 원 크기가 바뀌어도 간격을 기준으로 장식 개수를 자동 계산

**B. 개수 기준**
- 원 둘레에 정확히 N개 배치

---

## 5. 1색 / 2색 / 3색 순환 패턴

대상:
- Dot
- Sparkle
- Star
- Heart
- Ribbon
- Flower
- 사용자 업로드 이미지의 팔레트 틴트 모드

### 색상 수 선택
오른쪽 설정에 다음 버튼을 제공합니다.

`1색 | 2색 | 3색`

### 반복 예시
1색:
`핑크 → 핑크 → 핑크 → 핑크 ...`

2색:
`노랑 → 하늘 → 노랑 → 하늘 ...`

3색:
`노랑 → 하늘 → 핑크 → 노랑 → 하늘 → 핑크 ...`

각 색상은:
- 색상 선택기
- HEX 코드
- 스포이드

으로 변경할 수 있습니다.

색상이 실제로 어떤 순서로 반복되는지 보여주는 **패턴 시퀀스 미리보기 바**도 추가합니다.

---

## 6. 업로드 PNG의 색상 처리 방식

사용자 이미지 프레임에서는 두 가지 모드를 제공합니다.

### 원본 이미지 색상 유지
업로드한 PNG의 원래 색을 그대로 사용합니다.

적합한 경우:
- 이미 완성된 컬러 스티커
- 여러 색이 들어간 리본/꽃
- 캐릭터 아이콘

### 팔레트 색상 적용
PNG의 투명도/실루엣을 마스크처럼 사용하고 원래 RGB 색 대신 선택한 1~3개 팔레트 색으로 반복합니다.

예:
- 흰색 하트 PNG 하나 업로드
- 3색 선택: 노랑 / 하늘 / 핑크
- 결과: 노랑 하트 → 하늘 하트 → 핑크 하트 반복 프레임

---

## 7. 프리셋 UX 개편

- 프리셋 버튼의 의미를 `레이어 추가`에서 **`현재 프레임으로 사용`**으로 변경
- 상단에 `프리셋을 누르면 현재 디자인이 교체됩니다` 안내 표시
- 반복 패턴 계열 프리셋 강화
- Three Color Heart Ring 등 3색 예제 프리셋 추가
- 빈 커스텀 이미지 프리셋 카드는 제거하고 실제 업로드 박스로 대체

---

## 8. 유지되는 v0.2 기능

- 2000 × 2000 투명 PNG 출력
- Auto Fit
- HEX 색상 입력
- 스포이드
- 색상 템플릿
- Solid / Linear / Radial / Conic Gradient
- Glow / Bloom / Blur / Shadow
- Broken / Segmented / Arc
- Scribble / Rough / Brush
- Wavy / Scallop
- Glossy / Neon
- 실행취소 / 다시실행
- 프로젝트 JSON 저장 / 불러오기
- 내 프리셋 저장
- 브라우저 자동 저장

---

## 9. 이전 프로젝트 호환

v0.1 / v0.2 JSON을 불러오는 경우 과거 여러 레이어 전체를 다시 합성하지 않고, 당시 선택되어 있던 프레임 하나를 v0.3의 단일 디자인으로 가져옵니다.

v0.3에서 새로 저장한 프로젝트는 `version: 0.3` 구조를 사용합니다.

---

## 10. 배포 구조

- React
- TypeScript
- Vite
- 브라우저 Canvas 2D 렌더링
- 서버 없이 동작
- GitHub 업로드 가능
- Cloudflare Pages 배포 가능

Cloudflare Pages 권장값:

- Build command: `npm run build`
- Build output directory: `dist`
- Root directory: 비워두기

---

## 11. v0.3 완료 조건

- [x] 레이어 패널이 보이지 않는다.
- [x] 프리셋을 클릭할 때 프레임이 누적되지 않는다.
- [x] 항상 단일 프레임 하나만 편집한다.
- [x] 왼쪽 최상단에 PNG 업로드 박스가 보인다.
- [x] 업로드 즉시 원형 반복 프레임이 생성된다.
- [x] 간격/개수/크기/거리 조절이 가능하다.
- [x] 하트/리본/꽃 등에서 1~3색 반복을 선택할 수 있다.
- [x] 세 색을 각각 직접 수정할 수 있다.
- [x] 업로드 PNG는 원본 색 유지 또는 팔레트 틴트 모드를 선택할 수 있다.
- [x] 2000×2000 투명 PNG로 저장된다.
- [x] Cloudflare Pages용 `npm run build → dist` 구조를 유지한다.
